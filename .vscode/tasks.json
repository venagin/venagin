{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gmake_build",
            "type": "shell",
            "group": "build",
            "command": ["make build -f makefile"],
            "options": {"cwd": "${workspaceFolder}",}, 
            "isBackground": false           
        },
        {
            "label": "gmake_start",
            "type": "shell",
            "group": "build",
            "command": [
                "make start -f makefile"],
            "options": {"cwd": "${workspaceFolder}",}, 
            "isBackground": false
        },
    ]
}